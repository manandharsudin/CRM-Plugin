meta {
  name: 05 Customer message 1 (session auth)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/wp-json/stcrm/v1/tickets/{{ticketId}}/messages
  body: json
  auth: none
}

headers {
  Cookie: stcrm_session={{sessionCookie}}
}

body:json {
  {
    "message": "Customer reply 1 — waiting for a response."
  }
}

tests {
  test("status is 201", function() {
    expect(res.status).to.equal(201);
  });
}
